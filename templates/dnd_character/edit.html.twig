{% extends 'layout/base.html.twig' %}

{% block title %}Edit D&D character{% endblock %}

{% block body %}
    <h1>Edit D&D character</h1>

    {{ include('dnd_character/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('dnd_character_index') }}">back to list</a>

    {{ include('dnd_character/_delete_form.html.twig') }}
{% endblock %}
