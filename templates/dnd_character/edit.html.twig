{% extends 'layout/base.html.twig' %}

{% block title %}{{ dnd_character.name }} – edit D&D character{% endblock %}

{% block body %}

    <div class="row">
        <div class="col-md-8 offset-md-2 col-lg-6 offset-lg-3">
            <h1 class="d-inline-block">Edit D&D character</h1>
            <a class="btn btn-sm btn-primary float-right" href="{{ path('dnd_character_index') }}">
                <i class="fas fa-arrow-left"></i> Back to list
            </a>
            {{ include('dnd_character/_delete_form.html.twig') }}
            <hr>
        </div>
        <div class="col-md-8 offset-md-2 col-lg-6 offset-lg-3">
            {{ include('dnd_character/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
    </div>

{% endblock %}
